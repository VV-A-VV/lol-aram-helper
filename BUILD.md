# LOL ARAM Helper - 打包说明

## 打包步骤

1. 安装依赖：
```bash
pip install -r requirements.txt
```

2. 运行打包脚本：
```bash
python build.py
```

3. 打包完成后，可执行文件位于 `dist/LOL_ARAM_Helper.exe`

## 使用说明

双击 `LOL_ARAM_Helper.exe` 即可运行，无需安装 Python 环境。

程序会自动检测英雄联盟客户端，进入 ARAM 选人界面后会显示英雄推荐。

点击"更新数据"按钮可手动更新英雄数据。
